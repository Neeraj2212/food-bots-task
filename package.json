{
  "name": "express-server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build":"cd .. && yarn buiild",
    "install-clinet":"cd .. && yarn install",
    "heroku-postbuild":"yarn run install-client && yarn run build",
    "start": "node server.js",
    "dev":"nodemon server.js"
  },
  "author": "Neeraj Patil",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
